using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Saludar : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hola estoy saludando!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
